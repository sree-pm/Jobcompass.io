VITE_API_URL=http://localhost:8787
VITE_ANTHROPIC_PROXY=http://localhost:8787/api/llm
VITE_API_KEY=
# NOTE: Never put LLM API keys in VITE_ variables — they are bundled into the frontend.
# Route LLM calls through the API worker proxy instead.
