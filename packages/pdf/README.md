# @agentic-cv/pdf

PDF generation logic is currently implemented inline in `apps/api/src/lib/pdf.ts`.
This package is reserved for a future dedicated PDF rendering module using Cloudflare Browser Rendering.
